#!/bin/sh
# A simple wrapper shell script to specify the path to the import-attachment command.
# This shell script can be copied into a standard "bin" directory.

# Edit this variable to specify the abspath to the Distil installation.
DISTIL=/home/jboy/dev/distil/code

python ${DISTIL}/import_attachment_command.py "$@"
